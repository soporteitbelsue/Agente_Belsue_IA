import AgentWorkspace from "@/components/chat/AgentWorkspace";

/** Procedimientos internos: cómo trabajamos por dentro en la oficina. */
export default function ProcedimientosPage() {
  return <AgentWorkspace scope="procedimientos" />;
}
